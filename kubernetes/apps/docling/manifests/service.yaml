apiVersion: v1
kind: Service
metadata:
  name: docling
  namespace: docling
  labels:
    app: docling
    app.kubernetes.io/name: docling
    app.kubernetes.io/part-of: docling
spec:
  type: ClusterIP
  selector:
    app: docling
  ports:
    - name: http
      port: 80
      targetPort: 5001
      protocol: TCP
